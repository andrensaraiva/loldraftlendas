import{chromium}from'@playwright/test';const b=await chromium.launch({channel:'msedge'});for(const width of[1366,390]){const p=await b.newPage({viewport:{width,height:844}});await p.goto('http://127.0.0.1:5173');await p.getByRole('button',{name:'Começar draft'}).click();console.log(width,await p.evaluate(()=>Object.fromEntries(['.site-header','.draft-screen','.draft-progress','.team-strip','.section-heading','.section-heading h1','.section-heading p','.roll-panel','.player-card','.player-grid'].map(s=>{const e=document.querySelector(s),r=e.getBoundingClientRect(),c=getComputedStyle(e);return[s,{y:r.y,h:r.height,margin:c.margin,padding:c.padding,minHeight:c.minHeight,gap:c.gap}]}))));await p.close()}await b.close();
